backend:
  name: github
  repo: JDONATE38/siempredivinas-web 
  branch: main
  base_url: https://siempredivinas.com
  auth_endpoint: api/auth 

media_folder: "public/img"
public_folder: "/img"
locale: "es"

collections:
  # NUEVA COLECCIÓN: Para gestionar las secciones del catálogo con sus fotos
  - name: "categorias"
    label: "📁 Categorías del Catálogo"
    folder: "src/content/categorias"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Nombre para Menús", name: "title", widget: "string", hint: "Nombre que aparecerá en los menús de navegación."}
      - {label: "Título de la Página de Categoría", name: "pageTitle", widget: "string", hint: "Título principal que se mostrará en la página de la categoría."}
      - {label: "Descripción de la Categoría", name: "description", widget: "text", hint: "Contenido descriptivo para la página de la categoría."}
      - {label: "Orden de aparición (1, 2, 3...)", name: "order", widget: "number", default: 1}
      - {label: "SEO: Meta Título (Opcional)", name: "seoTitle", widget: "string", required: false, hint: "Título para los buscadores. Si se deja vacío, usará el 'Título de la Página de Categoría'."}
      - {label: "SEO: Meta Descripción (Opcional)", name: "seoDescription", widget: "text", required: false, hint: "Descripción para los buscadores."}
  
  # NUEVA COLECCIÓN: Para gestionar las subcategorías
  - name: "subcategorias"
    label: "📂 Subcategorías"
    folder: "src/content/subcategorias"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Nombre para Menús", name: "title", widget: "string", hint: "Nombre que aparecerá en los menús de navegación."}
      - {label: "Título de la Página de Subcategoría", name: "pageTitle", widget: "string", hint: "Título principal que se mostrará en la página de la subcategoría."}
      - {label: "Descripción de la Subcategoría", name: "description", widget: "text", hint: "Contenido descriptivo para la página de la subcategoría."}
      - {label: "Orden de aparición (1, 2, 3...)", name: "order", widget: "number", default: 1}
      - {label: "SEO: Meta Título (Opcional)", name: "seoTitle", widget: "string", required: false, hint: "Título para los buscadores. Si se deja vacío, usará el 'Título de la Página de Subcategoría'."}
      - {label: "SEO: Meta Descripción (Opcional)", name: "seoDescription", widget: "text", required: false, hint: "Descripción para los buscadores."}
      - label: "Categoría Principal"
        name: "parentCategory"
        widget: "relation"
        collection: "categorias"
        search_fields: ["title"]
        value_field: "title"
        display_fields: ["title"]


  - name: "productos"
    label: "👗 Productos"
    folder: "src/content/productos"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Nombre Producto", name: "title", widget: "string"}
      - {label: "Precio (€)", name: "price", widget: "number", value_type: "float"}
      
      # VINCULACIÓN: Aquí ella elegirá la categoría que creó arriba
      - label: "Categoría/Sección"
        name: "category"
      - label: "Subcategoría"
        name: "subcategory"
        widget: "relation"
        collection: "categorias"
        collection: "subcategorias"
        search_fields: ["title"]
        value_field: "title"
        display_fields: ["title"]

      - {label: "Estado del Stock", name: "stockStatus", widget: "select", options: ["En Stock", "Últimas Unidades", "Agotado"], default: "En Stock"}
      - {label: "Imagen Principal", name: "image", widget: "image"}
      - {label: "Foto Auxiliar 1 (Opcional)", name: "gallery1", widget: "image", required: false}
      - {label: "Foto Auxiliar 2 (Opcional)", name: "gallery2", widget: "image", required: false}
      - {label: "Foto Auxiliar 3 (Opcional)", name: "gallery3", widget: "image", required: false}
      
      # --- TALLAS (Híbrido) ---
      - {label: "Tallas (Selección Rápida)", name: "sizes", widget: "select", multiple: true, required: false, options: ["XS","S","M","L","XL","XXL","Única"]}
      - {label: "Otras Tallas (Escribir a mano)", name: "customSizes", widget: "string", required: false, hint: "Ej: 34, 46, Especial. Sepáralas por comas."}
      
      # --- COLORES (Híbrido) ---
      - {label: "Colores (Selección Rápida)", name: "colors", widget: "select", multiple: true, required: false, options: ["Negro", "Blanco", "Rojo", "Azul", "Verde", "Rosa", "Beige", "Amarillo", "Estampado", "Oro", "Plata"]}
      - {label: "Otros Colores (Escribir a mano)", name: "customColors", widget: "string", required: false, hint: "Ej: Verde Lima, Topos, Fucsia. Sepáralos por comas."}

      - {label: "Descripción", name: "description", widget: "text"}
      
      # --- CAMPOS SEO ---
      - {label: "SEO: Título Google (Opcional)", name: "seoTitle", widget: "string", required: false, hint: "Si lo dejas vacío, usará el nombre del producto"}
      - {label: "SEO: Descripción Google (Opcional)", name: "seoDescription", widget: "text", required: false, hint: "Resumen que saldrá en los resultados de búsqueda"}
      # -------------------------

      - {label: "Ocultar (Borrador)", name: "draft", widget: "boolean", default: false}